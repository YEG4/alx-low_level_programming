/**
 * mul - Multiplies a and b.
 * @a: first number
 * @b: second number
 *
 * Description: 'prints string on the console.'
 * Return: Always 0 (Success)
 */

int mul(int a, int b)
{
	return (a * b);
}
